@using Inventory.Repository.Paging;
@using Inventory.ViewModel.Shipment
@model PageResult<ShipmentTypeViewModel>
@{
    ViewData["Title"] = "Index";
}

<div class="row">
    <div class="col-6">
        Add New Shipment Type
        <a asp-action="Create" class="btn btn-primary">Add</a>
    </div>
    <div class="col-6"> Search Here...</div>
    <div class="row">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Shipment - Type Id
                    </th>
                    <th>
                        Title
                    </th>
                    <th>
                        Description
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach(var item in Model)
                {                    
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ShipmentTypeName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Description)
                        </td>
                        <td>
                            <a asp-route-id="@item.Id" asp-action="Edit" class="btn btn-primary">Edit</a>
                            <a asp-route-id="@item.Id" asp-action="Delete" class="btn btn-primary">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

